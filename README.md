# Dari
1. afg_channels.csv < for the channels in that region />
2. afgan_videos_collected.csv < all the video_ids_from_channels />
3. Dari-spreadsheet - video_data.csv < all the video_id with transcription check />
4. dari_filtered_data < videos that has `fa` transcript at least />
5. dari_transcripts_only.csv < videoes that can be confirmed as dari lang channels />

